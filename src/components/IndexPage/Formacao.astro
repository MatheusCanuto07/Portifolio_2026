---
import { Image } from "astro:assets";
import diplomaTecnico from "../../assets/diploma_tecnico.jpeg";
---

<section class="py-24 px-4 flex flex-col items-center gap-14">
  <!-- Título -->
  <div class="border-2 border-dashed border-primary px-6 py-2 rotate-1">
    <h2 class="text-3xl font-bold tracking-wide">Cursos & Certificações</h2>
  </div>

  <div class="w-full max-w-4xl flex flex-col gap-12">
    <article
      class="border-2 border-dashed p-6 rotate-[-0.7deg] flex flex-col lg:flex-row gap-6 items-start"
    >
      <div class="w-full lg:w-1/3 flex flex-col items-center">
        <div class="relative group cursor-pointer">
          <div
            class="border-2 border-dashed border-primary p-2 rotate-2 bg-base-100 shadow-lg"
          >
            <img
              src={diplomaTecnico.src}
              alt="Diploma senai"
              class="glightbox
               w-full object-contain
               max-h-64 md:max-h-80 lg:max-h-96
               mx-auto"
            />

            <div class="text-center mt-2 text-sm text-secondary">
              Clique para expandir
            </div>
          </div>
        </div>
      </div>
      <!-- Vai ser aplicado quando a tela for large ou maior -->
      <div class="w-full lg:w-2/3 text-center lg:text-left">
        <header class="mb-4">
          <h3 class="text-xl font-bold text-primary">
            Frontend Avançado com React
          </h3>
          <p class="text-sm text-secondary">
            Plataforma de Cursos · Concluído em 12/2024
          </p>
          <div
            class="flex justify-center lg:justify-start items-center gap-2 mt-2"
          >
            <div class="badge badge-primary badge-outline">120 horas</div>
            <div class="badge badge-outline">Certificado</div>
          </div>
        </header>

        <ul
          class="list-disc list-inside space-y-2 text-secondary leading-relaxed"
        >
          <li>
            Domínio de <strong>React Hooks</strong> avançados (useEffect, useContext,
            useReducer)
          </li>
          <li>
            Desenvolvimento de <strong>SPAs</strong> com React Router e gerenciamento
            de estado global
          </li>
          <li>
            Integração com <strong>APIs REST</strong> utilizando Axios e tratamento
            de erros
          </li>
          <li>
            Testes unitários e de integração com <strong
              >Jest e React Testing Library</strong
            >
          </li>
          <li>
            Projeto final: E-commerce completo com carrinho, checkout e
            autenticação
          </li>
        </ul>

        <div class="mt-4 flex justify-center lg:justify-start flex-wrap gap-2">
          <span class="badge">React</span>
          <span class="badge">TypeScript</span>
          <span class="badge">Tailwind CSS</span>
          <span class="badge">Jest</span>
        </div>
      </div>
    </article>
  </div>
</section>

<script>
  import GLightbox from "glightbox";
  import "glightbox/dist/css/glightbox.css";

  const lightbox = GLightbox({
    selector: ".glightbox",
    touchNavigation: true,
    loop: false,
    zoomable: true,
    draggable: true,
    openEffect: "zoom",
    closeEffect: "fade",
  });
</script>
